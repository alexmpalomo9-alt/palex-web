import { UnionNombreApellidoPipe } from './union-nombre-apellido.pipe';

describe('UnionNombreApellidoPipe', () => {
  it('create an instance', () => {
    const pipe = new UnionNombreApellidoPipe();
    expect(pipe).toBeTruthy();
  });
});
